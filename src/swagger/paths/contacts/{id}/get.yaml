parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string

operationId: getContactById
summary: Get contact by ID
description: Returns a contact by their ID.
security:
  - bearerAuth: []

responses:
  '200':
    description: Successful operation
  '404':
    description: Contact not found