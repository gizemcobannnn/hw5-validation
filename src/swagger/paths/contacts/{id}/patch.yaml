parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string

operationId: patchContactById
summary: Patch contact by ID
description: Update a contact by their ID.
security:
  - bearerAuth: []

responses:
  '200':
    description: Successful operation
  '404':
    description: Contact not found